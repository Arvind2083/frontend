export class Statement {
    constructor(
        transactionReference: number,
        iban: String,
        startBalance: number,
        MonetaryAmount: number,
        description: String,
        endBalance: number
    ) { }
}